<Form::Fieldset @label={{@label}} @hint={{@hint}} @error={{@error}}>
  {{yield
    (hash
      RadioField=(component
        (ensure-safe-component this.RadioFieldComponent)
        name=@name
        onChange=this.handleInput
        selectedValue=@value
      )
    )
  }}
</Form::Fieldset>