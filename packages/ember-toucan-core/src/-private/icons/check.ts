import templateOnlyComponent from '@ember/component/template-only';

export interface ToucanCheckIconComponentSignature {
  Args: {};
  Blocks: {
    default: [];
  };
  Element: SVGElement;
}

export default templateOnlyComponent<ToucanCheckIconComponentSignature>();
